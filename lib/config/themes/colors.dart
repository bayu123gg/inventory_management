import 'package:inventory_management/utils/packages.dart';

/// A class that defines the color palette for the application.
///
/// The [AppColors] class contains static constants for various colors used
/// throughout the application, ensuring a consistent color scheme.
class AppColors {
  // Application primary colors
  /// The primary color of the application.
  static const Color primaryColor = Color(0xFF2b2d42);

  /// The secondary color of the application.
  static const Color secondaryColor = Color(0xFF8d99ae);

  /// The tertiary color of the application.
  static const Color tertiaryColor = Color(0xFFedf2f4);

  /// The error color of the application.
  static const Color errorColor = Color(0xFFef233c);

  /// The accent color of the application.
  static const Color accentColor = Color(0xFFd90429);

  // Default universal colors
  /// The color for the white background.
  static const Color whiteBackground = Color(0xFFEFF1F0);

  /// The normal white color.
  static const Color white = Color(0xFFFFFFFF);

  /// The color used for links.
  static const Color linkColor = Color(0xFF00C4FF);

  /// The color used for disabled elements.
  static const Color disabledColor = Color(0xB3939393);
}
